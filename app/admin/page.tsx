"use client";import { useEffect,useState } from "react";type Court={id:string;name:string;hourly_rate:number;is_active:boolean};
export default function AdminPage(){const[code,setCode]=useState("");const[courts,setCourts]=useState<Court[]>([]);const[msg,setMsg]=useState("");const load=()=>fetch("/api/admin/courts").then(r=>r.json()).then(d=>setCourts(d.courts||[]));useEffect(()=>{load();},[]);async function save(c:Court){setMsg("");const r=await fetch("/api/admin/courts",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({adminCode:code,courtId:c.id,hourlyRate:c.hourly_rate,isActive:c.is_active})});const d=await r.json();setMsg(r.ok?`${c.name} updated.`:(d.error||"Update failed"));}return <main className="container"><section className="hero"><div className="logoBox">BUELO</div><div><span className="pill">ADMIN</span><h1>Court Settings</h1><p>Change hourly rates per court without editing code.</p></div></section><section className="card"><label>Admin access code</label><input type="password" value={code} onChange={e=>setCode(e.target.value)} placeholder="Enter ADMIN_ACCESS_CODE"/></section><div className="grid grid-2" style={{marginTop:16}}>{courts.map(c=><section className="card" key={c.id}><h2>{c.name}</h2><label>Hourly rate (PHP)</label><input type="number" value={c.hourly_rate} onChange={e=>setCourts(p=>p.map(x=>x.id===c.id?{...x,hourly_rate:Number(e.target.value)}:x))}/><div className="row" style={{marginTop:14}}><label style={{margin:0}}><input style={{width:"auto"}} type="checkbox" checked={c.is_active} onChange={e=>setCourts(p=>p.map(x=>x.id===c.id?{...x,is_active:e.target.checked}:x))}/> Active</label><button className="btn btn-primary" onClick={()=>save(c)}>Save</button></div></section>)}</div>{msg&&<p>{msg}</p>}</main>;}
